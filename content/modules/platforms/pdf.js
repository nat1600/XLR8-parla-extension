// modules/platforms/pdf.js

// Module to handle PDF text selection interactions
const ParlaPDF = {
    
    // Setup function to initialize PDF text selection handling
    setup() {
      console.log('📄 PDF detected - Enhanced text selection enabled');
      // No additional setup needed for PDFs at this time
    }
  };
  
// Expose ParlaPDF globally
  window.ParlaPDF = ParlaPDF;